model FlaggedKeyword {
    id          String   @id @default(cuid())
    keyword     String   @unique
    replaceWord String?
    category    String?
    isActive    Boolean  @default(true)
    createdAt   DateTime @default(now())
    updatedAt   DateTime @updatedAt

    @@map("flagged_keywords")
}
